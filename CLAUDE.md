stock trading related scripts. use python script if possible.
when use python, install and run in conda env. called "trader".
write test cases before the actual code. naming like this: test_xxx.py
if there's any scecrets, store in ".env" file.